PORT=3333

# Docker Postgres — run from repo root: pnpm run db:up
DATABASE_URL=postgres://sikai:sikai_dev@localhost:5432/sikai

# Coach: OpenAI chat completions (optional — returns 503 if unset)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
