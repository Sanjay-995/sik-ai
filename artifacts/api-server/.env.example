PORT=3333
# Optional: PostgreSQL for persisted scans (otherwise in-memory)
# DATABASE_URL=postgres://user:pass@localhost:5432/sikai

# Coach: OpenAI chat completions (optional — returns 503 if unset)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
